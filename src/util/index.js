export const nameFromType = (type) => {
  return {}
}

export const paddingSize = (
  top,
  bottom,
  left,
  right,
) => {
  return {
    paddingTop: top,
    paddingBottom: bottom,
    paddingLeft: left,
    paddingRight: right,
  }
}
export const marginSize = (
  top,
  bottom,
  left,
  right,
) => {
  return {
    marginTop: top,
    marginBottom: bottom,
    marginLeft: left,
    marginRight: right,
  }
}